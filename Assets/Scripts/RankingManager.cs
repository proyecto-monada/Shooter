using System.Collections;
using System.Collections.Generic;
using System.Text;
using UnityEngine;

/*

public class RankingManager : MonoBehaviour
{
    public struct Ranked {
		char[] r_name;
		unsigned int r_num;
	}
	
	
	// Start is called before the first frame update
    void Start()
    {
		private static char [5] name;
		public static byte[] All_bytes ReadAllBytes(string File_Rank);
		while(All_bytes){
			i = 0;
			while(All_bytes != "*"){
				name[i] = To.Char (All_bytes[i]);
				i++;
				name++;
			}
			
		}
    } 

}
*/